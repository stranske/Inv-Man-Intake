name: Agents PR Health

on:
  workflow_dispatch: {}

jobs:
  health:
    uses: stranske/Workflows/.github/workflows/reusable-agents-pr-health.yml@main
    with:
      dry_run: true
      max_prs: 10
    secrets: inherit
